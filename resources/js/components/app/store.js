const state = () => {
    return {
        //
    }
};

const mutations = {};
const getters = {};
const actions = {};

export default {
    namespaced: true,
    state,
    getters,
    mutations,
    actions,
}